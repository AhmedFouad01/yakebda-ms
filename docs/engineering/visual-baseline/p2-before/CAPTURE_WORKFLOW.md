# Automated baseline capture

The baseline pack is generated by `scripts/p2-capture-visual-baseline.mjs` through the temporary P2 visual workflow. Generated PNG files and `manifest.json` are the source of truth for before/after parity.
